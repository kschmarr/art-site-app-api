CREATE TABLE art (
    artid INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    title TEXT NOT NULL,
    description TEXT NOT NULL,
    price INTEGER NOT NULL,
    dims TEXT NOT NULL,
    image TEXT NOT NULL
);